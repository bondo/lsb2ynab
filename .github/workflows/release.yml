on:
  push:
    tags:
      - "v*"

name: Create Release

jobs:
  artifacts:
    name: Build for ${{ matrix.platform.os_name }}
    runs-on: ${{ matrix.platform.runner }}
    strategy:
      matrix:
        platform:
          - os_name: Linux-x86_64
            runner: ubuntu-latest
            target: x86_64-unknown-linux-musl
            bin: lsb2ynab
            archive_cmd: tar -czf
            archive_ext: tar.gz
          - os_name: Windows-x86_64
            runner: windows-latest
            target: x86_64-pc-windows-msvc
            bin: lsb2ynab.exe
            archive_cmd: 7z a -tzip
            archive_ext: zip
          - os_name: MacOS-x86_64
            runner: macos-latest
            target: x86_64-apple-darwin
            bin: lsb2ynab
            archive_cmd: tar -czf
            archive_ext: tar.gz
          - os_name: MacOS-aarch64
            runner: macos-latest
            target: aarch64-apple-darwin
            bin: lsb2ynab
            archive_cmd: tar -czf
            archive_ext: tar.gz
    steps:
      - name: Determine version
        id: version
        run: printf "tag=%s\n" "${REF#refs\/tags\/}" >> "$GITHUB_OUTPUT"
        env:
          REF: ${{ github.ref }}
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ steps.version.outputs.tag }}
      - name: Set up Rust
        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: 1.80.0
          target: ${{ matrix.platform.target }}
      - name: Build
        run: cargo build --release --target ${{ matrix.platform.target }}
      - name: Archive
        run: |
          mv target/${{ matrix.platform.target }}/release/${{ matrix.platform.bin }} .
          ${{ matrix.platform.archive_cmd }} lsb2ynab-${{ steps.version.outputs.tag }}-${{matrix.platform.target}}.${{ matrix.platform.archive_ext }} ${{ matrix.platform.bin }}
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: lsb2ynab-${{ steps.version.outputs.tag }}-${{matrix.platform.target}}.${{ matrix.platform.archive_ext }}
          path: lsb2ynab-${{ steps.version.outputs.tag }}-${{matrix.platform.target}}.${{ matrix.platform.archive_ext }}
  release:
    name: Create Release
    runs-on: ubuntu-latest
    needs: artifacts
    permissions:
      contents: write
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: lsb2ynab-*
          merge-multiple: true
      - name: Publish GitHub release
        uses: softprops/action-gh-release@v2
        with:
          files: lsb2ynab-*
          fail_on_unmatched_files: true
          generate_release_notes: true
